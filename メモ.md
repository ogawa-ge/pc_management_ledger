社内のPC管理台帳アプリ
・ログイン機能
    Microsoftアカウントでのログイン実施
・アカウントには権限を付与する
    管理者
    ユーザー
・管理者
    PCの一覧確認
    ユーザー一覧確認
    ユーザーを指定してPCの代理登録
・ユーザー
    自分のPCを登録可能
・PC登録について
    PCスペックを簡単に入力可能
        入力画面にターミナルを起動するボタンを実装
        ターミナルからスペック情報を出力するコマンドを入力し、フォームに登録する
        登録されたデータはGemini APIを使用して必要なデータのみ登録する

追加
・ユーザー
    PC返却フォーム
        返却日
        返却理由
        PCの状態（初期化済みなどプルダウンで選択可）
    未使用PC一覧画面
・PC登録について
    管理番号を自動採番
        ノートパソコンの場合N-001、N-002などNから始まる3桁の数字
        デスクトップパソコンの場合D-001、D-002などDから始まる3桁の数字
        現時点でD-001～D-007、N-001～N-034までExcelで管理されているため、データ移行が必要


2. **Given** PC登録画面を開いている, **When** デスクトップパソコンを選択し、スペック情報を入力して登録する, **Then** Gemini APIで必要な情報が抽出され、「D-XXX」形式の管理番号が付与されて登録される
 
→
ユーザーは自分のPC情報の編集が可能。
新規移行したデータ（D-001～D-007、N-001～N-034）は管理者がEXCELからインポートしたデータのため情報が古い可能性がある。
初期利用時にユーザーは自分のデータを確認し、新規登録と同様にターミナルからPC情報を上書きする必要あり。
↑データ移行は不要。試用中の人は必ず登録してもらうでもいい。入力が楽なら。
 
1. **Given** 管理者としてログインしている, **When** ユーザーを指定してPC情報を登録する, **Then** 指定したユーザーのPCとして情報が登録され、適切な管理番号が付与される
 
→
管理者／ユーザーの権限情報は
本システム内で設定する。Microsoftアカウントの権限は利用しない。設定する画面は不要で権限を管理しているテーブルの値の変更を手動でメンテナンスすることを想定

 
2. **Given** 一般ユーザーとしてログインしている, **When** PC一覧またはユーザー一覧にアクセスしようとする, **Then** アクセスが拒否される
→
一般ユーザーはPC一覧は一覧は見えてOK。未使用のPCは見て判断できるようにはしたい。
管理者は一覧をcsvファイルでダウンロードできる。
アプリはスマホでの利用は想定していない。PCのみで利用を想定している。

 
- **PC**: 管理番号（N-XXX/D-XXX）、所有者（User）、PC種別（ノート/デスクトップ）、ステータス（利用中/未使用/廃棄など）、スペック情報（CPU、メモリ容量、ストレージ容量、OSバージョン、メーカー名、モデル名）を持つ
→
ステータスは
利用中/未使用/廃棄待ち/廃棄済み
廃棄待ち：故障等で社内にある状態
廃棄済み：すでに処分し社内からなくなった状態。廃棄済みでも管理台帳には情報を残しておく。
廃棄待ちから廃棄済みの変更は管理者ができる。

 







`spec-kit`を利用した**Spec-Driven Development (SDD)**の開始ですね。技術者としてのこだわり（ハイブリッド構成・最新サービス採用）を反映した、`plan`コマンドに最適なコンテキスト用テキストを作成しました。

この内容を `context.md` などのファイルに保存して読み込ませるか、プロンプトの冒頭に貼り付けて使用してください。

---

## PC資産管理システム：実装コンテキスト

### 1. システム概要
PCの貸出・返却を管理するWebアプリケーション。ユーザー数は20〜30名と小規模で、アクセスは特定のタイミング（貸出時・返却時）に限定される。低コスト運用と、モダンな技術スタックによる学習・経歴への貢献を両立させる。

### 2. 技術スタック
* **Frontend:** Next.js (Hosted on AWS Amplify)
* **Backend (Hybrid):**
    * **AWS Lambda (Python/FastAPI):** 認証（Login）、軽量なAPI、ECSの起動トリガーを担当。
    * **Amazon ECS Express Mode (Python/FastAPI):** 資産管理のコアロジック、管理者用機能など、メモリ/CPUを要する処理を担当。
* **Database:** Amazon DynamoDB (NoSQL)
* **IaC:** AWS CDK (Python) による全リソースの定義。

### 3. アーキテクチャの要点
* **イベント駆動型スケーリング:** * 通常時はECSのタスク数を0に設定し、コストを最小化。
    * ユーザーがLambda経由でログインに成功した際、LambdaからAWS SDKを用いてECSサービスを `desiredCount=1` に更新し、コンテナを起動させる。
* **コスト最適化:**
    * アクセスがない時間はLambdaの実行費用のみ（ほぼ0円）。
    * 夜間や週末はスケジュールベースでECSを完全停止。
    * ALB（Load Balancer）を介さず、Lambda Function URLsやECSの直接接続を活用して固定費を削減する。

### 4. 主要な機能要件
1.  **認証機能:** ユーザーログイン（Lambda担当）。
2.  **資産一覧表示:** 管理PCの一覧と現在のステータス表示。
3.  **貸出・返却フロー:** PCのQRコード読み取り、またはID入力によるステータス変更。
4.  **管理者ダッシュボード:** 貸出履歴の確認、資産の新規登録（ECS担当）。

### 5. 制約事項・優先順位
1.  **コスト:** 月間数百円〜数千円以内を目指す。
2.  **UX:** ECS起動時の待機時間（コールドスタート）に対し、フロントエンドで適切なローディング表示を行う。
3.  **拡張性:** 将来的にユーザー数が増えた際、ECS側のリソースを拡張しやすい設計にする。

---

### 次のステップへのアドバイス

このコンテキストを `spec-kit plan` に渡すと、AIは「LambdaとECSの役割分担」や「DynamoDBのテーブル設計」を考慮したスペックファイルを生成し始めます。

次に進む際は、以下のように指示を出すのがスムーズです：

> 「このコンテキストを基に、まずは **DynamoDBのテーブル定義** と、**LambdaからECSを起動するPythonコードのインターフェース** を含む仕様書（Spec）を作成してください。」

`spec-kit` で具体的にどの機能（例えば「貸出処理」など）から定義を固めていきたいですか？